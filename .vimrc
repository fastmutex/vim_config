" size of a hard tabstop
colorscheme robinhood2
syntax on
filetype plugin on      
set number
set autoindent
set cindent
set tabstop=8
set t_Co=256

" always uses spaces instead of tab characters
" set expandtab

" size of an "indent"
set shiftwidth=8

map <F2> :retab <CR> :wq! <CR>

